# ── 실행 모드 ──
# "api"   → Claude API 사용 (MVP, 빠른 프로토타이핑)
# "local" → 로컬 fine-tuned 모델 사용
MODE=api

# ── Claude API 키 (MODE=api 사용 시) ──
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxx

# ── OpenAI API 키 (대체 옵션) ──
# OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxx

# ── 서버 설정 ──
HOST=0.0.0.0
PORT=8000
